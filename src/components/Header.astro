---
const { pathname } = Astro.url
const normalizedPathName = 
    pathname.length > 1 && pathname.endsWith("/") ? pathname.slice(0, -1) : pathname

const pages = [
    { name: "Principal", href: "/" },
    { name: "Robots", href: "/robots", active: normalizedPathName.startsWith("/robots") },
    { name: "Categorías", href: "/categorias" },
    { name: "Inscribete", href: "/inscripciones"}
]
---


<header class="mb-10 h-16 max-w-[100vw] lg:h-24">
    <nav class="group flex h-full w-full items-center justify-between px-10 lg:justify-center">
        
    </nav>
</header>